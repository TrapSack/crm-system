import React from "react";
import styled from "styled-components";

export default function SiteLogo() {
  return <LogoContainer>Site Name</LogoContainer>;
}

const LogoContainer = styled.div`
  width: 100px;
`;
